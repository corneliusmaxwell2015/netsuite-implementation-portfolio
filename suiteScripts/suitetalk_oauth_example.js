/**
 * Example only — not production ready
 */
define([], () => {

    const example = () => {
        return {
            message: 'OAuth example placeholder for portfolio.'
        };
    };

    return { example };
});

